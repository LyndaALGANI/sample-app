pipeline {
  agent any
  stages {
    stage('Build') {
      steps {
        echo '▶ Démarrage de la compilation'
      }
    }
    stage('Test') {
      steps {
        echo '▶ Exécution des tests (simulation)'
      }
    }
    stage('Deploy') {
      steps {
        echo '▶ Simulation de déploiement'
      }
    }
  }
}
